/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["cdn.dribbble.com", "images.unsplash.com"], // Combine domains into one array
  },
};

export default nextConfig;
